[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
addopts = -v -p no:warnings --cov=app --cov-report=term-missing --cov-report=html:htmlcov --ignore=tests/benchmark_performance.py --ignore=tests/verify_tasks.py
cov_fail_under = 40
timeout = 30
